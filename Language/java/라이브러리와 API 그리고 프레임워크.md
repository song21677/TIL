# 라이브러리와 API 그리고 프레임워크
## 1. 라이브러리
소프트웨어를 개발할 때, 컴퓨터 프로그램이 사용하는 비휘발성 자원의 모임이다. 구성 데이터, 문서, 도움말 자료, 메시지 틀, 미리 작성된 코드, 서브루틴(함수), 클래스, 값, 자료형 사양 등을 포함할 수 있다.

예를 들어 잘 만들어진 클래스 하나를 소스코드 공개없이 공유하고 싶을 때 (C++ 경우)헤더파일과 컴파일된 목적 파일, 그리고 사용 방법에 대한 문서를 제공해줄 수 있는데 이것들을 통틀어 라이브러리라고 한다.

<br>

## 2. API(Appllication Programming Interface)
실무 개발에서는 프로그램을 혼자 밑단부터 윗단까지 모두 개발할 수 없다. 따라서 서로간의 협업이나 이미 만들어진 소프트웨어 컴포넌트를 결합해서 만드는데, 라이브러리도 그 중 하나이다. 그러한 컴포넌트들을 결합하기 위한 매개체가 API이다. (컴포넌트를 활용하는 규약)

예를 들면, 한 클래스에 정의된 메소드를 실행한다 했으 때, 해당 메소드의 필요한 인자나 리턴 타입이 해당 라이브러리의 API가 된다고 할 수 있다.

### 라이브러리가 아닌 API
구글 클라우드에서 제공하는 Speech API가 있다.

음성 데이터를 넣으면 그 데이터를 원하는 언어의 텍스트로 인식해서 돌려주는 방식이다. 그리고 기계학습을 이용한 인공지능 기술로 해당 기능을 구현하였다고 하고, 내부적인 동작은 공개하지 않고 있다. 그리고 여기서 제공하는 RESTful API의 경우는 특정한 포맷에 맞추어서 HTTP 음성 데이터를 포함한 요청을 보내면 HTTP 응답으로 해석해낸 텍스트가 돌아오는 방식이다. 이 때 HTTP 요청/응답 포맷 역시 API이며, 이는 개발자의 로컬 컴퓨터에 설치된 라이브러리를 통해 제공받는 방식이 아니다. 외부 원격에 있는 서버에서 부터 이러한 음성 인식 서비스를 제공받는 것이다.

이 경우는 라이브러리가 아니지만 API를 제공한다.

<br>

## 3. 프레임워크
어떠한 문제를 해결해나가는 데 사용하는 틀이다. 안드로이드 프레임워크를 이용하면 앱을 만들 수 있다.

### 프레임워크와 라이브러리 차이
일반적으로 프레임워크 안에 라이브러리가 포함되는 경우가 많다. 프레임워크는 보통 어떤 거대한 응용프로그램 등을 만들기 위한 틀인 경우가 많다. 따라서 이를 위해 미리 코딩된 코드조각들(라이브러리)은 프레임워크에 거의 당연히 들어가있다고 볼 수 있다.

라이브러리만 가져다 쓰는 경우는, 프로그램의 메인은 개발자가 직접 쓰고, 중간중간 필요한 부분만 라이브러리를 가져와서 쓰는 형태인 반면, 프레임워크를 가져다 쓰는 경우, 그 프레임워크의 본 목적과 다른 프로그램은 만들 수 없다. (안드로이드 프레임워크로 웹 서버를 만들 수 없다.)

프레임워크가 라이브러리에 비해 해 주는 일이 더 많고, 규모가 거대한 만큼 원래 목적과 다른 일은 할 수 없다는 점이 다르다고 볼 수 있다.

### Reference
* http://eine.tistory.com/entry/%EB%9D%BC%EC%9D%B4%EB%B8%8C%EB%9F%AC%EB%A6%AC-API-ABI-%EB%9C%BB-%EB%B9%84%EA%B5%90-%EC%A0%95%EB%A6%AC